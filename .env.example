# Dev server configuration
PORT=3000
BACKEND_URL=http://localhost:8080

# Frontend application configurations
# Toggle default state: 'mock' or 'live' when local storage is empty
VITE_API_MODE=mock

# Direct API target URL (optional, leave blank to use Vite /api proxy)
VITE_API_URL=
